{
  "name": "needhave",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "setup": "mkdir tmp/ log/",
    "start": "rails server",
    "clean": "rm -r node_modules/ && rm yarn.lock",
    "postinstall": "yarn clean && cd frontend && yarn install"
  }
}
